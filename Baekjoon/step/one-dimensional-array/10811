N, M = map(int, input().split()) # N(바구니 개수), M(뒤집는 횟수) 입력받음
baskets = list(range(1, N + 1)) # 1부터 N까지 번호가 적힌 바구니 리스트

for _ in range(M):
    i, j = map(int, input().split())
    # baskets[i-1:j] = baskets[i-1:j][::-1]
    baskets[i-1:j] = baskets[i-1:j][::-1]

print(*baskets)
